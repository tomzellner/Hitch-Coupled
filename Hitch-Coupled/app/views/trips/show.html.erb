<% if current_user == @driver %>
	<h1>Welcome to Your Trip </h1>

	<h2>From: <%= @trip.start_city %>, <%= @trip.start_state %></h2>
	<h2>To: <%= @trip.end_city %>, <%= @trip.end_state %></h2>
	<h2>Start Date: <%= @trip.start_date.to_time.strftime('%B %e 20%y') %></h2>
	<h2>End Date: <%= @trip.end_date.to_time.strftime('%B %e 20%y') %></h2>
		<span> Seats in car: <%= @trip.car.seats %> </span>

	<div style="border: thin solid black" id = 'driver_profile'>
		<%= @driver.first_name %>
		<%= @driver.last_name %><br>
		<%= @driver.phonenumber %>

		<% @trip.passengers.each do |p| %>
			<div style="border: thin solid black" > <%= p.first_name %> <%= p.last_name %> </div>
		<% end %>

		<%= link_to 'View Profile', user_path %>

	</div>

<% elsif user_signed_in? %>

<%= @trip.passengers.count %>


<h1>Welcome to <%= @driver.first_name %> <%= @driver.last_name %>'s Trip </h1>

<h2>From: <%= @trip.start_city %>, <%= @trip.start_state %></h2>
<h2>To: <%= @trip.end_city %>, <%= @trip.end_state %></h2>
<h2>Start Date: <%= @trip.start_date.to_time.strftime('%B %e 20%y') %></h2>
<h2>End Date: <%= @trip.end_date.to_time.strftime('%B %e 20%y') %></h2>
	<span> Seats in car: <%= @trip.car.seats %> </span>

<div style="border: thin solid black" id = 'driver_profile'>
	<%= @driver.first_name %>
	<%= @driver.last_name %><br>
	<%= @driver.phonenumber %>

</div>

<% @trip.passengers.each do |p| %>
			<div style="border: thin solid black" > <%= p.first_name %> <%= p.last_name %> </div>
		<% end %>

<%= link_to "add passagger", trip_add_passenger_path(@trip) %>

<% else %>

<h1>LOG IN YA BISHHHH </h1>

<% end %>
